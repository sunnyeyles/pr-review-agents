# Pin pnpm install behaviour for this workspace so local installs match CI.
# Workspace packages (@pr-review/*) are wired via symlinks in each project's
# node_modules, which requires pnpm's default isolated linker.
node-linker=isolated
symlink=true
package-import-method=auto
